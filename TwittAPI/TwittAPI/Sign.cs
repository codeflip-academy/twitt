using System;

namespace twitt
{
    public class Sign
    { 
        public void SignInValidation(string userName, string password)
        {
            var userInput = "";
            var userNameInput = "";
            //query the database for password and the userName

           
            //put the exececution of the database into a variable for the password and the userName

           
            //compare the variables to the userInput

            
             //If true then run the method that views all the post or the profile page.
            
             //send error message
            
        }
        
        public void SignOut()
        {
            //query the database for the state of the sign in the profile

            //if the state is signed out then run the sign in method.
        }

        public void SignUp()
        {
            //Post a prompt asking the user for their information: Full name, User name, picture, and, password.

            //Get the information that was inputed from the prompt and add it to the database.
        }
    }

}
